{
  "name": "e-promt",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && powershell -Command \"Remove-Item docs -Recurse -Force -ErrorAction SilentlyContinue; mkdir docs; xcopy dist docs /E /I /Y\""
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "typescript": "^5.3.0"
  }
}
